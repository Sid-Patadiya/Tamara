export enum Authentication {
  REDIRECT_LOGIN = 'redirectToLogin',
  TOKEN = 'token',
  USER_INFO = 'userInfo',
  GUEST_USER = 'guestUser',
  TIME = 'getTime',
}

export enum ThemeEnums {
  LIGHT = 'light',
  DARK = 'dark',
}
